// This Jenkinsfile is RETIRED. CI/CD is fully handled by GitHub Actions
// (.github/workflows/ci.yml) since the Jenkins->GHA migration (#41).
// Kept as a one-line tombstone so git log retains the history.
//
// Former pipeline: typecheck -> smoke (two wrangler dev servers) -> deploy on main.
// See git log for the full Jenkinsfile history.
